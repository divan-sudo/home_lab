sudo chmod -R 777 /mnt
sudo chmod -R 777 /mnt/homeserver/Movies
sudo chmod -R 777 /mnt/homeserver

to check permisions or to just add them
- seams like after updating the docker image plex lost its priviliges on the mnt folder for movies or mnt in general